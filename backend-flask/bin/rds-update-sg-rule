#! /bin/bash

CURRENT_IP=$(curl ifconfig.me)

aws ec2 modify-security-group-rules \
     --group-id $RDS_SG_ID \
    --security-group-rules "SecurityGroupRuleId=$RDS_SG_RULE_ID,SecurityGroupRule={Description=DEV_CONTAINER_IP,IpProtocol=tcp,FromPort=5432,ToPort=5432,CidrIpv4=$CURRENT_IP/32}"